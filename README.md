# danielnathanael-Apptest

This app is using **redux** as state management.

To run unit test, please run `npm test tests/api.test.js` or `jest tests/api.test.js` if jest is configured to run in terminal.
